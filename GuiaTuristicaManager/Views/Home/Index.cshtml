@{
    ViewData["Title"] = "Inicio";
}


@*
    <div id="Render">
        <div ng-controller="TableController">
            <div layout="row" layout-sm="column" style="margin-top: 5em !important;">
                <div flex="25">
                </div>
                <div flex="50">
                    <md-toolbar class="md-table-toolbar md-default">
                        <div class="md-toolbar-tools">
                            <span>Zonas</span>
                        </div>
                    </md-toolbar>

                    <!-- exact table from live demo -->
                    <md-table-container>
                        <table md-table md-row-select multiple ng-model="selected" md-progress="promise">
                            <thead md-head md-order="query.order" md-on-reorder="getDesserts">
                                <tr md-row>
                                    <th md-column md-order-by="nameToLower"><span>Nombre</span></th>
                                    <th md-column><span>Acciones</span></th>
                                </tr>
                            </thead>
                            <tbody md-body>
                                <tr md-row md-select="dessert" md-select-id="name" md-auto-select ng-repeat="dessert in desserts">
                                    <td md-cell>{{dessert.Name}}</td>
                                    <td md-cell>{{dessert.Id}}</td>
                                </tr>
                            </tbody>
                        </table>
                    </md-table-container>

                    <md-table-pagination md-limit="query.limit" md-limit-options="[5, 10, 15]" md-page="query.page" md-total="{{desserts.count}}" md-on-paginate="getDesserts" md-page-select></md-table-pagination>
                </div>
                <div flex="25">
                </div>
            </div>
        </div>
    </div>

    <script>
        var Table = angular.module('Render', []);
        Table.controller('TableController', function ($scope) {
            $scope.selected = [];

            $scope.query = {
                order: 'name',
                limit: 5,
                page: 1
            };
            $scope.desserts = [
                    {
                        Name: 'La bufa',
                        Id: 1,
                    },
                    {
                        Name: 'Teatro Calderon',
                        Id: 2
                    }
                ];
            function success() {
                $scope.desserts
            }

            $scope.getDesserts = [
                    {
                        Name: 'La bufa',
                        Id: 1,
                    },
                    {
                        Name: 'Teatro Calderon',
                        Id: 2
                    }
                ];
        });
    </script>
*@